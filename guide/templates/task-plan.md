# {Step名} — 作業計画

## 判断ポイント

<!-- ユーザーと確定した設計判断を記録する。未確定の場合は「未確定」と明記し、確定してから成果物作成に入る -->

| # | 論点 | 決定 |
|---|------|------|
| 1 | {論点の内容} | {決定内容 または「未確定」} |

## 共通前提・着手条件

<!-- 並列タスクに着手させる前に固定すべき前提を記録する -->
<!-- 前提が未確定のまま並列着手すると、後続の統合コストが増える -->

- **共通契約**: {例: エラー形式 / ID 命名 / 共通用語 / 生成物配置 / 共通ヘルパー配置}
- **担当境界**: {どこまでが各担当の編集対象か}
- **着手条件**: {何が確定したら各担当が着手してよいか}
- **禁止事項**: {共通基盤の独断変更禁止、担当外ファイルの直接編集禁止 など}
- **参照先**: {全担当が参照する共通ファイルやディレクトリ}

## Phase 構成

<!-- Phase ごとにタスク一覧を記載する。各タスクには成果物・入力・担当エージェント・依存関係を明記する -->
<!-- 並列実行可能なタスクは同一 Phase にまとめる -->
<!-- タスク粒度原則: 1成果物ファイル = 1タスク。1タスクが複数ファイルを生成してはならない。 -->
<!-- 下流工程（テスト設計・実装）が成果物単位でパイプラインを回せる粒度にする。 -->

### 各 Phase の進め方

<!-- 計画に必ず含めること -->

**実行前:**
1. 並列タスクがある場合、共通ルール・判断ポイントの適用箇所・タスクごとの要点を整理する
2. 整理した内容をサブエージェントのプロンプトに含めて委譲する
3. 並列タスクがある場合、担当境界・マージ順・トレーサビリティ更新対象を明記する

**実行後:**
1. **内部レビュー**: Phase 成果物の整合性チェック
   - 下流が困る問題があれば修正 → 再レビュー（PASS まで繰り返す）
2. **ユーザーにコミットを提案**
3. **codex レビュー**（/codex-review）: コミット後に実行
   - 指摘があれば対応 → 再レビュー（LGTM まで繰り返す）
4. 次の Phase に進む

### Phase 1（逐次）

| タスク | 成果物 | 入力（主要） | エージェント | 状態 |
|--------|--------|-------------|------------|------|
| {タスクID}: {タスク名} | `{1つの成果物ファイル名}` | {入力ファイルパス} | {エージェント名} | 未着手 |
| {タスクID}-R1: 内部レビュー | — | — | {reviewer} | 未着手 |
| {タスクID}-R2: codex レビュー | — | — | codex | 未着手 |

成果物配置先: `{パス}`

タスクごとの補足:

| タスク | 担当境界 | 依存タスク | ブランチ | 完了条件 | レビュー時の明記項目 |
|--------|----------|-----------|----------|----------|----------------------|
| {タスクID} | {編集対象・対象外} | {依存タスクID または `-`} | {ブランチ名} | {このタスク単体の完了条件} | {影響範囲、未解決前提、更新した対応表 など} |

### Phase 2（並列）

| タスク | 成果物 | 入力（主要） | エージェント | 状態 |
|--------|--------|-------------|------------|------|
| {タスクID}: {タスク名} | `{1つの成果物ファイル名}` | {入力ファイルパス} | {エージェント名} | 未着手 |
| {タスクID}: {タスク名} | `{1つの成果物ファイル名}` | {入力ファイルパス} | {エージェント名} | 未着手 |
| {タスクID}-R1: 内部レビュー | — | — | {reviewer} | 未着手 |
| {タスクID}-R2: codex レビュー | — | — | codex | 未着手 |

成果物配置先: `{パス}`

タスクごとの補足:

| タスク | 担当境界 | 依存タスク | ブランチ | 完了条件 | レビュー時の明記項目 |
|--------|----------|-----------|----------|----------|----------------------|
| {タスクID} | {編集対象・対象外} | {依存タスクID} | {ブランチ名} | {このタスク単体の完了条件} | {影響範囲、未解決前提、更新した対応表 など} |
| {タスクID} | {編集対象・対象外} | {依存タスクID} | {ブランチ名} | {このタスク単体の完了条件} | {影響範囲、未解決前提、更新した対応表 など} |

### マージ・統合ルール

<!-- 並列タスクがある場合は必須 -->

- **マージ順**: {依存グラフに従った順序}
- **共通部品変更時の扱い**: {Lead 判断必須 / 別タスク化 / 先行マージ など}
- **再確認条件**: {依存先を取り込んだ後に何を再実行・再レビューするか}

### 共有ファイル運用表

<!-- 高競合ファイルごとに運用ルールを固定する -->

| 対象 | 初回作成担当 | 他担当の追記可否 | 競合時一次対応責任者 | 直列化要否 |
|------|------------|----------------|-------------------|-----------|
| {対象ファイル/領域} | {担当名} | {不可 / 指揮役承認で可} | {責任者} | {直列 / 並列可} |

## トレーサビリティ更新対象

<!-- 設計・テスト・実装のいずれでも使用可 -->
<!-- 成果物単位で、どの対応関係を更新・明記するかを定義する -->

- **更新対象 1**: {例: 要件 ID ↔ 設計ファイル}
- **更新対象 2**: {例: テストケース ID ↔ テストコード}
- **更新対象 3**: {例: テストケース ID ↔ 実装コード}
- **記録場所**: {成果物内コメント / 対応表 / レビュー依頼文 / 別紙}

## 依存グラフ

<!-- Phase 間・タスク間の依存関係を ASCII またはテキストで図示する -->

```
{依存グラフ}
```

## 品質基準

<!-- レビュー時にチェックされる観点を列挙する -->

- **下流作業可能性**: この成果物だけで下流工程が曖昧さなく作業できるか（唯一の判定基準）
- **上流整合性**: 入力として参照した上流成果物と矛盾がないか
- **タスク間整合性**: 並列タスクの成果物間で用語・ID・参照関係が統一されているか
- **共通前提遵守**: 共通契約・担当境界・禁止事項が守られているか
- **トレーサビリティ**: 定義した更新対象の対応関係が追跡可能か
- **MVP スコープ**: `deliverables/docs/02_scope.md` の範囲内か
- **用語集準拠**: `dev-journal/deliverables/docs/01_glossary.md` の用語を使用しているか
- **完了条件**: work-breakdown に定義された完了条件を満たしているか

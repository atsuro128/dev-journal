# work-breakdown の保証種別が test_strategy.md の実態と乖離している

## 発見日
2026-04-12

## カテゴリ
testing

## 影響度
中

## 発見経緯
user-report

## 関連ステップ
Step 6（テスト設計）/ Step 11（システムテスト・UAT）

## ブロッカー
なし

## 問題

Step 6 の work-breakdown（`ai-dev-framework/guide/work-breakdown/step6-testing/main.md`）では保証種別を「正常系・異常系・テナント分離・認可・状態遷移」の5分類と定義しているが、実際の test_strategy.md（`deliverables/docs/60_test/test_strategy.md:331`）では「正常系 / 異常系 / 境界値 / セキュリティ / 性能 / 認可 / 状態遷移 / テナント分離」の8分類に拡張されている。

さらに、テストケースの実態ではこれ以外にも以下の観点が暗黙的にカバーされている:
- 排他制御（楽観的ロック 409）
- ページネーション
- UI 状態（ローディング、エラー表示）
- キャッシュ整合性

一方、以下の観点は traceability.md でカバー扱いだが実ケースが不足している:
- CORS / セキュリティヘッダ（HSTS, X-Content-Type-Options, X-Frame-Options）
- 真の競合シナリオ（同時承認・同時支払のレース再現）
- 組み合わせテスト（ロール × 所有者 × 状態 × テナント境界）
- タイムゾーン（UTC保存/JST表示）
- 二重送信防止
- `/health` エンドポイント

## 影響

- 正本（work-breakdown）の記述を信じると、実態より狭い観点でテスト設計を評価してしまう
- traceability.md でカバー扱いの観点に実ケースがなく、品質保証の穴になる

## 提案

1. work-breakdown の保証種別を test_strategy.md の8分類に合わせて更新する
2. 不足している観点のうち MVP で重要なもの（CORS/セキュリティヘッダ、タイムゾーン、二重送信防止）のテストケースを cross-cutting.md に追加する
3. traceability.md の「カバー済み」表記を実態に合わせて修正する

---

## 解決内容

### スコープ判断

本 issue の対応は**最小スコープ**とし、以下のみに絞った:

1. **work-breakdown の保証種別を 8 分類に統一** — 正本（`test_strategy.md §8`）との整合性確保
2. **`traceability.md` の虚偽カバー標記を修正** — 実態に合わせて参照を「-」に変更し、追跡先（issue 081）と管理方式検討先（ops-080）を備考欄に明記

当初の提案 #2（不足観点のテストケース追加）は本 issue に含めず、**別 issue として切り出した**。理由:
- 各観点（CORS/セキュリティヘッダ、タイムゾーン、/health、真の競合シナリオ、二重送信防止）は要件 ID 状態・優先度・要件遡及の必要性が異なる
- 073 の本来の主題（保証種別の乖離整理）を超えるスコープ拡大を避ける
- MVP 完了後のワークフロー（管理方式）が未定のため、個別対応より先に管理方式の決定が必要

### 派生 issue

- **`ops-080`**: MVP スコープ外事項の管理方式決定（起票のみ、対応は別セッション）
- **`issue 081`**: MVP スコープ外のテスト観点一括管理（5 観点を §1〜§5 で詳細記録）
  - §1: CORS / セキュリティヘッダ
  - §2: タイムゾーン（UTC 保存 / JST 表示）
  - §3: /health エンドポイント
  - §4: 真の競合シナリオ（楽観ロック 409 レース再現）— 要件遡及必要
  - §5: 二重送信防止 — 要件遡及必要（要件自体が不在）

### 実施内容

#### 1. work-breakdown step6-testing の保証種別統一

`ai-dev-framework/guide/work-breakdown/step6-testing/main.md`:
- **L72**: 「保証種別: 正常系・異常系・テナント分離・認可・状態遷移のどれを保証するケースかを明示」→ 「正常系・異常系・境界値・セキュリティ・性能・認可・状態遷移・テナント分離のどれを保証するケースかを明示（`test_strategy.md` §8 の 8 分類と整合）」
- **L171**: 「正常系・異常系・テナント分離・認可・状態遷移のいずれか」→ 「正常系・異常系・境界値・セキュリティ・性能・認可・状態遷移・テナント分離のいずれか。`test_strategy.md` §8 準拠」
- **L269**: 「保証種別（正常系・異常系・テナント分離・認可・状態遷移）」→ 「保証種別（正常系・異常系・境界値・セキュリティ・性能・認可・状態遷移・テナント分離。`test_strategy.md` §8 準拠）」

`ai-dev-framework/guide/work-breakdown/step6-testing/review.md`:
- **L77**: 「各テストケースに保証種別（正常系・異常系・テナント分離・認可・状態遷移）が明示されているか」→ 「（8 分類、`test_strategy.md` §8 準拠）が明示されているか」

#### 2. `traceability.md` の虚偽カバー標記修正

`dev-journal/deliverables/docs/60_test/traceability.md`:
- **L224 (NFR-SEC-006 CORS)**: BE テスト反映先を `CRS-076〜088` → `-` に変更。備考欄に「MVP スコープ外。`issue 081 §1` で追跡、管理方式は `ops-080` で検討中」を追加
- **L225 (NFR-SEC-007 セキュリティヘッダ)**: 同上（`issue 081 §1` を共有）
- **L233 (NFR-AVAIL-002 /health)**: 備考欄を「Step 9 以降で実装時にヘルスチェックテストを追加」→「MVP スコープ外。`issue 081 §3` で追跡、管理方式は `ops-080` で検討中」に変更
- **L246 (NFR-DATA-007 タイムゾーン)**: 備考欄を「自動テスト対象外（アプリケーション実装規約で保証。Step 9 以降でタイムスタンプ形式の検証を含める）」→「MVP スコープ外（手動補強: `smoke_check.md SMK-070/071`）。`issue 081 §2` で追跡、管理方式は `ops-080` で検討中」に変更

### traceability.md に載らない観点

真の競合シナリオ（`issue 081 §4`）と二重送信防止（`issue 081 §5`）は**要件 ID が存在しない**ため、`traceability.md` には記載できない（traceability.md は要件 ID ベースのマッピング表）。issue 081 §4 §5 で追跡し、要件遡及後（ops-080 対応の一環または別 issue）に `traceability.md` へ追加される。

### レビュー

本対応は指揮役直接修正（機械的置換中心、設計判断は備考欄文言のみで軽微）。内部 reviewer / codex レビューは省略（workflow.md の「小規模 → 指揮役直接修正」基準に該当、スコープが極めて明確）。

## 解決日
2026-04-13

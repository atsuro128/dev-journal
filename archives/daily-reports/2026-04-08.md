## 2026-04-08 作業日報

### 実施内容

#### root-project
- codex-review スキルに `git fetch origin` を追加（古い ref 参照の防止）
- エージェント・スキルの work-breakdown 参照パスを新ディレクトリ構造に更新
- DevContainer の CLI バージョン固定を廃止し、エイリアス設定を `setup-shell-aliases.sh` に分離

#### ai-dev-framework
- work-breakdown を `review.md` / `main.md` の 2 ファイル構成ディレクトリに分割（ops-047）
- 全 work-breakdown から「次 Step への受け渡し契約」セクションを廃止（ops-050）
- Step 8/9/10 の work-breakdown に横断レビュータスク (X) を追加
- Step 10 の作業ルールを BE/FE 並列実装に変更
- workflow.md に CI 修正の閾値ルールを追加（小規模エラーは指揮役直接修正）

#### expense-saas
- 9-E（明細テスト）実装 → PR マージ
- 9-F（ワークフローテスト）実装 → PR マージ
- 9-G（添付ファイルテスト）実装 → PR #14 マージ
- 9-X 横断レビュー指摘 3 件対応:
  - 098: テストケース ID トレーサビリティマッピング追加（PR #16）
  - 099: FE ページスタブを最小構造に拡張（PR #19、25/25 PASS）
  - 100: `auth_service_test` の DB 依存除去（PR #15）
- staticcheck キャッシュ問題対応 → PR #18 で一時回避 → PR #20 で golangci-lint に移行

#### dev-journal
- Step 10（機能実装）チケット起票・進捗管理更新
- Step 9 完了（2026-04-08）、9-X 完了を progress.md に反映
- review-findings 098/099/100 を resolved/ に移動
- ops-050（受け渡し契約廃止）、ops-047（work-breakdown 分割）、ops-063 の解決
- セッションログ更新、issue 059（FE テストファイルパス慣習不一致）起票

### 判断・決定事項
- **codex の Hook テスト要件**: `vi.mock` でフックモジュール全体を差し替えると必ず指摘される。実 Hook を import して `globalThis.fetch` のみモックする方針をエージェントプロンプトに明示すべき
- **codex が古い ref を読む問題**: worktree の `origin/branch-name` トラッキング ref は `git fetch origin` しないと更新されない。codex-review スキルに反映済み
- **CI 修正の閾値ルール**: 3 ファイル以下・各 5 行以下の小規模エラーは指揮役が直接修正、それ以上はエージェントに差し戻し（workflow.md に明文化）
- **重大問題はユーザー確認**: 9-X 099 対応で「ブランチ消失」と誤判断し PR #17 を独断クローズ → 実際は残存。自走中でも既存作業破棄はユーザーに確認すべき（memory に記録済み）
- **codex 押し返しは PR コメントに残す**: codex exec のプロンプトだけでは記録が残らない
- **codex 指摘の品質ゲート評価**: Step 10 スコープの機能実装を Step 9 スタブに要求する指摘は押し返せる。ただし「テストを通すために仕様を壊した」指摘は妥当
- **横断レビュータスクのテンプレート化**: 並列実装のある Step（5/8/9/10）に X タスクとして組み込む。個別レビューでは見えない整合性問題を拾える
- **意思決定**: `useAttachmentDownload` は `useQuery` + `staleTime: 0`、`useAttachments` は `ApiResponse<Attachment[]>`、ReportForm の `min(1)` バリデーション復元 + RPT-FE-025 テスト修正、golangci-lint v2.1 採用

### 明日以降のタスク
- **Step 10（機能実装）** に着手
  - work-breakdown の確認とチケット起票から開始
  - BE/FE 並列実装ルールに従う

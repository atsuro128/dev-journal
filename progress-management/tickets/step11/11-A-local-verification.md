# ローカル動作確認

- 担当: ユーザー + 指揮役
- 依存: Step 10 全完了（10-A〜10-G）、Step 6-D 完了
- ブランチ: なし（修正が必要な場合は issue 経由で対応）
- 出力先: issue コメントまたは進捗管理ディレクトリ内
- テンプレート: なし

## 入力

| 資料 | パス | 参照箇所 |
|------|------|----------|
| スモークチェックリスト | deliverables/docs/60_test/manual_checklists/smoke_check.md | 全54項目（確認項目の正本） |
| Docker Compose 設定 | expense-saas/docker-compose.yml | 全体 |
| 画面仕様（各機能） | deliverables/docs/50_detail_design/screens/*.md | チェックリストが参照する画面の詳細 |

## 責務

- docker compose up でフロントエンド・API・DB をローカル起動し、ブラウザからアクセスできることを確認
- **smoke_check.md の全54項目を実施**（ロール別導線、通信中の挙動、エラー表示、添付ファイルのブラウザ実挙動、レスポンシブ、日本語 UI、タイムスタンプ、ページネーション/フィルタ、キャッシュ整合性）
- 各項目の実施結果を記録（PASS / FAIL / スキップ理由）
- 発見した問題を issue として起票（ブロッカー / 非ブロッカーを分類）
- 含めない:
  - 業務フロー成立性の検証（E2E 自動テスト 11-C の領域）
  - テストコードの実装、デプロイ

## 完了条件

- docker compose でフロントエンド・API・DB が起動し、ブラウザからアクセスできる
- smoke_check.md の全54項目が実施され、結果が記録されている
- ブロッカーとなる問題がない（または修正済み）

## 実施順序

ロール切替コストを最小化し、かつデータ依存（後続ロールがフィクスチャ状態を書き換える）を考慮して、**Member 先行 → 状態変更ロール後行** の順で実施する。セッションをまたぐ場合も以下の順序で再開する。

進捗トラッキング: 進行中の Phase と次に実施する SMK ID は `dev-journal/progress-management/11-A-smoke-results.md` の「現在地」欄に記録する。

### 設計原則

1. **Member を最優先で潰す** — ボリューム最大（約 37 項目）の役割を先に終わらせ、進捗を稼ぐ
2. **データ依存の順序を守る** — Approver SMK-011（承認）や Accounting SMK-092（支払）はフィクスチャ状態を変更する。`reportSubmitted` / `reportApproved` を参照する Member 項目はこれより**前**に実施する
3. **未ログイン項目は最後** — 全ロール完了後にログアウト状態でまとめて実施

### Phase 1: ロール入口確認（4 アカウント順番にログイン）
- SMK-001（Member 入口）→ SMK-002（Approver 入口）→ SMK-003（Accounting 入口）→ SMK-004（Admin 入口）
- 必然的にロール 4 回切替が発生する Phase

### Phase 2: Member で連続実施（最大ボリューム）
Member ロールでログインし、以下を連続実施。フィクスチャはまだ初期状態。

- §4.1 ナビ・導線: SMK-005（ナビ非表示）, SMK-007（403 リダイレクト）, SMK-009（ログアウト後の戻る）
  - SMK-008（未ログインリダイレクト）は Phase 5 へ
- §4.2 通信中: SMK-010, 012, 013, 014（SMK-011 は Phase 3 へ）
- §4.3 エラー表示: SMK-020〜026, 028, 029（SMK-027 は Phase 3 へ）
- §4.4 添付ファイル: SMK-030〜038
- §4.5 レスポンシブ: SMK-060〜062（SMK-063 は Phase 3 へ）
- §4.6 日本語 UI: SMK-050〜053
- §4.7 タイムスタンプ: SMK-070〜072
- §4.8 ページネーション: SMK-080〜084
- §4.9 キャッシュ: SMK-093, 094

### Phase 3: Approver で連続実施
- SMK-006（ナビ非表示）→ SMK-011（承認二重押下）→ SMK-027（409 状態競合）→ SMK-063（スマホ幅承認）→ SMK-091（承認後 pending 一覧整合）
- SMK-011 で `reportSubmitted` を承認し状態を変更するため、Member の関連項目は Phase 2 で完了済みであること

### Phase 4: Accounting で連続実施
- SMK-092（支払後 payable 一覧整合）

### Phase 5: 未ログインで実施
- SMK-008（未ログインで `/dashboard` を直接開きリダイレクト確認）
- SMK-090（未ログインで `/health` 直接アクセス）

### 順序変更時の注意

- Phase 内の項目順は前提データの整合性を保つこと
- 順序を変えた場合は本セクションを更新し、`11-A-smoke-results.md` の「現在地」欄も更新する
- フィクスチャの状態が崩れた場合は `make seed` で再投入可能（冪等）

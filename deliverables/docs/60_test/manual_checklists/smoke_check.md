# 手動チェックリスト: 開発者スモーク確認（SMK）

## この文書の役割

| 項目 | 内容 |
|------|------|
| 目的 | Step 11-A（開発者スモーク確認）で使用する手動チェック観点を定義する |
| 正本情報 | チェックID（SMK-XXX）、確認観点、対象、操作手順、期待結果 |
| 扱わない内容 | 業務フロー成立性の検証（E2E 自動テストの領域）、ユーザー受け入れ観点（`uat_check.md` の領域） |
| 主な参照元 | `60_test/traceability.md`, `60_test/test_strategy.md`, `60_test/test_cases/*.md`, `50_detail_design/screens/*.md` |
| 主な参照先 | Step 11-A（システムテスト・スモーク） |

---

## 1. 責務境界

### 含む観点

- ブラウザ実挙動（クリック挙動、二重押下防止、フォーカス制御）
- 表示崩れ・レスポンシブ（PC / スマホ幅）
- 文言の自然さ（日本語ラベル・トースト・エラーメッセージ）
- 体感導線（ロール別の入口画面、403 時の挙動、非表示要素）
- HTTP エラー時のフロント表示（401/403/404/409/422 のトースト・フォームエラー）
- 添付ファイルのブラウザ実挙動（5MB 境界、MIME 判定、ダウンロード起動）
- タイムスタンプ表示（JST 整合）
- ページネーション・フィルタの操作感
- キャッシュ／再読込整合性

### 含まない観点（自動テスト or UAT で担保）

- ロジック検証・状態遷移検証 → BE / FE 自動テストでカバー（traceability.md 参照）
- 業務フロー成立性（UC-* の達成可否） → E2E 自動テスト + `uat_check.md` でカバー
- データの永続化・テナント分離の確認 → BE 自動テスト（cross-cutting）でカバー
- 認可ロジック（ロール × リソース） → BE 自動テスト + RBAC マトリクスでカバー

## 2. 重複排除の根拠

`traceability.md` を走査し、以下を手動チェック対象として抽出した。

| 区分 | 抽出根拠 | 対応するチェックID |
|------|---------|-------------------|
| BE/FE 両方が `-` | NFR-PERF-003（軽量スモーク代替） | SMK-080 |
| BE/FE 両方が `-` | NFR-AVAIL-002（ヘルスチェック手動確認） | SMK-090 |
| BE/FE 両方が `-` | NFR-DATA-007（タイムゾーン JST 表示） | SMK-070, SMK-071 |
| BE/FE 両方が `-` | NFR-UX-004（クライアントサイドリアルタイムバリデーション） | SMK-020〜SMK-024 |
| BE/FE 両方が `-` | NFR-UX-005（API 通信中のローディング表示） | SMK-010〜SMK-013 |
| FE 側 `-`（補強） | NFR-UX-001（レスポンシブ）—自動 E2E は主要フロー検証のため、画面崩れの目視確認は手動が必須 | SMK-060〜SMK-063 |
| FE 側 `-`（補強） | NFR-UX-002（日本語固定）—自動 E2E は文言完全一致が困難 | SMK-050〜SMK-053 |
| FE 側 `-`（補強） | NFR-UX-003（操作結果の即時フィードバック）—トースト/フォームエラーの位置・色・体感は目視 | SMK-030〜SMK-035 |

NFR-PERF-001/002 は BE 自動テスト（CRS-083〜088）で担保済みのため手動チェックには含めない。
NFR-AVAIL-001/003 は運用領域のため本リストでは扱わない（運用設計の責務）。
NFR-DATA-006（UTF-8）は DB 設定で保証されるためマイグレーション手順書の責務とし、本リストでは扱わない。

## 3. 利用フィクスチャ

`test_strategy.md` §4 に準拠。本リストでは以下を使用する。

| 変数名 | テナント | ロール | 用途 |
|--------|---------|--------|------|
| `userMember` | テナントA | Member | 申請者導線 |
| `userApprover` | テナントA | Approver | 承認者導線 |
| `userAccounting` | テナントA | Accounting | 経理導線 |
| `userAdmin` | テナントA | Admin | 管理者導線 |
| `reportDraft` | テナントA | - | draft 状態のサンプルレポート |
| `reportSubmitted` | テナントA | - | submitted 状態のサンプルレポート |
| `reportApproved` | テナントA | - | approved 状態のサンプルレポート |

## 4. チェックリスト

### 4.1 ロール別導線（SMK-001〜009）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-001 | ログイン直後の入口画面（Member） | ログイン → ダッシュボード | `userMember` | Member | ログイン画面 | 1. メール/パスワードを入力 2. ログインを押下 | ダッシュボード画面に遷移し、Member 用の集計（自分の draft/submitted/rejected 件数）が表示される。承認待ち件数・支払待ち件数のカードは表示されない | 開発者 |
| SMK-002 | ログイン直後の入口画面（Approver） | ログイン → ダッシュボード | `userApprover` | Approver | ログイン画面 | 1. ログイン | ダッシュボードに承認待ち件数のカードが追加表示される | 開発者 |
| SMK-003 | ログイン直後の入口画面（Accounting） | ログイン → ダッシュボード | `userAccounting` | Accounting | ログイン画面 | 1. ログイン | ダッシュボードに支払待ち件数のカードが追加表示される | 開発者 |
| SMK-004 | ログイン直後の入口画面（Admin） | ログイン → ダッシュボード | `userAdmin` | Admin | ログイン画面 | 1. ログイン | テナント全体集計（全ステータス件数・メンバー数・月別合計）が表示される | 開発者 |
| SMK-005 | グローバルナビの非表示要素（Member） | グローバルナビゲーション | `userMember` | Member | ダッシュボード | 1. グローバルナビを目視 | 「承認待ち」「支払待ち」「テナント設定」などへの導線が表示されていない | 開発者 |
| SMK-006 | グローバルナビの非表示要素（Approver） | グローバルナビゲーション | `userApprover` | Approver | ダッシュボード | 1. グローバルナビを目視 | 「承認待ち」のメニューは表示されるが「支払待ち」「テナント設定」は表示されない | 開発者 |
| SMK-007 | 認可エラー時のリダイレクト（403） | 直接 URL アクセス | `userMember` | Member | ダッシュボード | 1. ブラウザで `/settings/tenant` を直接開く | 403 画面または「権限がありません」のトーストが表示され、ダッシュボードにリダイレクトされる | 開発者 |
| SMK-008 | 未ログイン時のリダイレクト | 任意の保護画面 | なし | （未ログイン） | ログイン画面 | 1. ログアウト状態で `/dashboard` を直接開く | ログイン画面にリダイレクトされる。元の遷移先が return_to に保持される | 開発者 |
| SMK-009 | ログアウト後の戻る操作 | ログアウト | `userMember` | Member | ダッシュボード | 1. ログアウト 2. ブラウザの戻るボタンを押下 | ダッシュボードを再描画せず、ログイン画面のまま留まる（API は 401 を返却し、再ログイン要求になる） | 開発者 |

### 4.2 通信中の挙動（SMK-010〜014）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-010 | レポート提出時のローディング表示 | レポート詳細 → 提出 | `reportDraft`（明細1件以上） | Member | レポート詳細画面 | 1. 「提出」を押下 2. 確認ダイアログで「提出する」を押下 | ボタンが disable になりスピナー表示され、レスポンス受信後に成功トーストが表示される | 開発者 |
| SMK-011 | 二重押下防止（承認） | 承認待ち詳細 → 承認 | `reportSubmitted` | Approver | 承認待ちレポート詳細 | 1. 「承認」を素早く 2 回連続でクリック | 1 回目のクリックでボタンが disable になり、2 回目のクリックは API リクエストを発火しない | 開発者 |
| SMK-012 | 添付アップロード中のローディング | 明細詳細 → ファイル添付 | `reportDraft`（明細1件以上） | Member | 明細詳細画面 | 1. 領収書ファイル（PDF, 4MB）を選択 | アップロード進行中はプログレス or スピナーが表示され、完了後に一覧へ即時反映される | 開発者 |
| SMK-013 | 一覧読み込み中のスケルトン/スピナー | レポート一覧 | `userMember`（レポート 30 件） | Member | レポート一覧 | 1. 画面を再読込 | データ取得中にスケルトン or スピナーが表示され、取得後に一覧が描画される | 開発者 |
| SMK-014 | 通信中の操作不可化（保存中の編集禁止） | レポート編集 | `reportDraft` | Member | レポート編集画面 | 1. 「保存」を押下 2. 通信中にタイトルを書き換えようとする | 保存処理中は入力フォームが disable になり、入力できない | 開発者 |

### 4.3 エラー表示（SMK-020〜029）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-020 | リアルタイムバリデーション（必須） | レポート作成 | なし | Member | レポート作成画面 | 1. タイトルを未入力でフォーカスを外す | フォーカスアウト時にフィールド下に「タイトルを入力してください」と赤字で表示される | 開発者 |
| SMK-021 | リアルタイムバリデーション（範囲） | 明細追加 | `reportDraft` | Member | 明細追加モーダル | 1. 金額に `0` を入力してフォーカスを外す | 「金額は正の整数で入力してください」とフィールド下に赤字で表示される | 開発者 |
| SMK-022 | リアルタイムバリデーション（日付論理） | レポート作成 | なし | Member | レポート作成画面 | 1. 開始日に `2026-04-30` を入力してフォーカスアウト 2. 終了日に `2026-04-01` を入力してフォーカスアウト 3. 開始日のみを `2026-05-01` 等に修正してフォーカスアウト | 開始日 blur / 終了日 blur のいずれの操作でも、開始日フィールド下に「開始日は終了日以前を指定してください」、終了日フィールド下に「終了日は開始日以降を指定してください」が同時に表示される。条件違反が解消されると両フィールドのエラー表示が同時に消える（issue #141） | 開発者 |
| SMK-023 | 422 サーバーサイドバリデーション | 明細追加 | `reportDraft` | Member | 明細追加モーダル | 1. 金額に小数値を含む値を入力（FE バリデーションを迂回するため DevTools でフォーム改変） 2. 保存 | 422 レスポンスを受け、フィールド下に「金額は正の整数で入力してください」が赤字で表示される | 開発者 |
| SMK-024 | 401 セッション切れ | 任意操作 | `userMember`（JWT 期限切れ） | Member | レポート一覧 | 1. アクセストークン期限切れ後にレポート詳細を開く | 自動でリフレッシュトークン交換が走り、成功時はそのまま画面遷移。失敗時はログイン画面にリダイレクトされ「セッションが切れました」とトースト表示 | 開発者 |
| SMK-025 | 403 権限不足のトースト | 直接 URL | `userMember` | Member | ダッシュボード | 1. `/approvals` を直接開く | 「この画面にアクセスする権限がありません。」とトースト表示し、ダッシュボードに戻る | 開発者 |
| SMK-026 | 404 存在しないリソース | レポート詳細 | なし | Member | レポート一覧 | 1. URL のレポートIDを存在しない UUID に書き換える | 画面内に EmptyState で「指定されたデータが見つかりません。」と表示、「レポート一覧に戻る」リンクが表示される | 開発者 |
| SMK-027 | 409 状態競合 | 承認 | `reportSubmitted` を別タブで先に承認 | Approver | 承認待ち詳細 | 1. 別タブで承認 2. 元のタブで「承認」押下 | 「他のユーザーがこのレポートを更新しました。ページを再読み込みしてください。」とトースト表示 | 開発者 |
| SMK-028 | バックエンド不達時の挙動 | 任意操作 | なし | Member | レポート一覧 | 1. ターミナルで `docker compose stop api` で API を停止 2. SPA 内で任意の操作（ダッシュボード遷移等）を行う | Snackbar（エラー）に「サーバーとの通信に失敗しました。しばらくしてから再度お試しください。」が表示される | 開発者 |
| SMK-029 | エラートースト/フォームエラーの自動消去 | 任意操作 | なし | Member | レポート一覧 | 1. 422 エラーを発生させトースト表示 2. 数秒待機 | 成功トーストは数秒後に自動消去される。エラートーストは手動クローズまで表示が残る（または画面遷移時に消える） | 開発者 |

### 4.4 添付ファイルのブラウザ実挙動（SMK-030〜038）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-030 | JPEG アップロード正常系 | 明細 → 添付 | `reportDraft`（明細1件以上） | Member | 明細詳細 | 1. JPEG（1MB）を選択 | 一覧に追加される | 開発者 |
| SMK-031 | PNG アップロード正常系 | 明細 → 添付 | 同上 | Member | 明細詳細 | 1. PNG（1MB）を選択 | 一覧に追加される | 開発者 |
| SMK-032 | PDF アップロード正常系 | 明細 → 添付 | 同上 | Member | 明細詳細 | 1. PDF（2MB）を選択 | 一覧に追加される | 開発者 |
| SMK-033 | ファイル形式エラー（拡張子） | 明細 → 添付 | 同上 | Member | 明細詳細 | 1. .docx ファイルを選択 | 「JPEG, PNG, PDF のみアップロード可能です」とエラー表示。アップロードは実行されない | 開発者 |
| SMK-034 | ファイルサイズ境界（5MB ちょうど） | 明細 → 添付 | 同上 | Member | 明細詳細 | 1. 5,242,880 バイト（5MB ちょうど）の PDF を選択 | アップロード成功する（境界値が許可側） | 開発者 |
| SMK-035 | ファイルサイズ境界（5MB + 1byte） | 明細 → 添付 | 同上 | Member | 明細詳細 | 1. 5,242,881 バイトの PDF を選択 | 「ファイルサイズは5MB以下にしてください」とエラー表示。アップロードは実行されない | 開発者 |
| SMK-036 | MIME タイプ偽装の検出 | 明細 → 添付 | 同上 | Member | 明細詳細 | 1. 非対応ファイル形式（例: `.txt` や `.exe` 等）を `.pdf` にリネームしたファイルを選択 | サーバーが MIME タイプを検証し、422 エラー「JPEG, PNG, PDF のみアップロード可能です」が表示される | 開発者 |
| SMK-037 | プレビュー・ダウンロード起動 | 添付一覧 | `reportSubmitted`（添付あり） | Member（所有者） | レポート詳細 | 1. 添付ファイルの「ファイル名」テキストを押下 2. ↓ アイコンを押下 | 1. ファイル名テキスト押下: クリック同期で新しいタブが開き、取得完了後に画像/PDF がブラウザ内で表示される（Content-Disposition: inline）。2. ↓ アイコン押下: 新しいタブは開かず、ブラウザのダウンロードが即起動する（Content-Disposition: attachment）。ファイル名・拡張子が正しい | 開発者 |
| SMK-038 | 添付削除（draft のみ） | 添付一覧 | `reportDraft`（添付あり） | Member | 明細詳細 | 1. 添付ファイルの「削除」を押下 2. 確認ダイアログで「削除」 | 一覧から消え、トーストで成功表示 | 開発者 |

### 4.5 レスポンシブ表示（SMK-060〜063, 101〜102）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-060 | PC 幅（1280px）の主要画面 | ダッシュボード/レポート一覧/レポート詳細 | `userMember`（複数レポート） | Member | ダッシュボード | 1. ブラウザを 1280px 幅で表示 2. 各画面を巡回 | テーブル列が崩れず、サイドナビが常時表示される | 開発者 |
| SMK-061 | スマホ幅（375px）の主要画面 | 同上 | 同上 | Member | ダッシュボード | 1. DevTools でスマホ幅（iPhone SE）に切り替え 2. 各画面を巡回 | サイドナビがハンバーガーに収納され、テーブルがカード表示またはスクロール可能になる | 開発者 |
| SMK-062 | スマホ幅でのフォーム操作 | レポート作成・明細追加 | なし | Member | レポート作成画面 | 1. スマホ幅で入力フォームを操作 | 入力フィールドが画面幅に収まり、キーボード表示時にも操作対象が隠れない | 開発者 |
| SMK-063 | スマホ幅での承認操作 | 承認待ち詳細 | `reportSubmitted` | Approver | 承認待ち一覧 | 1. スマホ幅で詳細を開き「承認」押下 | ボタンがタップ可能領域として十分な大きさで表示される | 開発者 |
| SMK-101 | スマホ幅でのテナント全レポート一覧テーブル表示 | テナント全レポート一覧（SCR-ADM-001） | `userAdmin`（レポート複数件） | Admin | テナント全レポート一覧 | 1. DevTools でスマホ幅（375px）に切り替え 2. テーブルの列（申請者名/タイトル/合計金額/ステータス/提出日）を目視確認 | テーブルが横スクロール可能になり、列の内容が重なって読めなくなることがない。ハンバーガーメニューが表示される | 開発者 |
| SMK-102 | スマホ幅でのテナント情報カード表示 | テナント情報（SCR-ADM-002） | `userAdmin` | Admin | テナント情報画面 | 1. DevTools でスマホ幅（375px）に切り替え 2. 会社名カード・Phase 3 予告メッセージの表示を目視確認 | カードが画面幅 100% に収まり、テキストが途切れない。ハンバーガーメニューが表示される | 開発者 |

### 4.6 日本語 UI（SMK-050〜053）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-050 | ラベル・ボタンの日本語 | 全画面 | なし | Member | ダッシュボード | 1. 各画面を巡回 | すべてのラベル・ボタン文言が日本語で、英語混じりや未翻訳の placeholder が無い | 開発者 |
| SMK-051 | エラーメッセージの自然さ | バリデーションエラー | なし | Member | レポート作成画面 | 1. SMK-020〜023 の状態を再現 | エラー文言が「を入力してください」「以下にしてください」のように日本語として自然 | 開発者 |
| SMK-052 | トースト文言の自然さ | 成功・失敗トースト | なし | Member | レポート作成 → 保存 | 1. 正常系・異常系を実行 | トースト文言（例: 「レポートを保存しました」「保存に失敗しました」）が日本語として自然 | 開発者 |
| SMK-053 | 状態ラベル | レポート一覧 | 各ステータスのレポート | Member | レポート一覧 | 1. ステータス列を確認 | 「下書き」「提出済み」「承認済み」「却下」「支払済み」のように日本語で表示される（status の英語値が露出しない） | 開発者 |

### 4.7 タイムスタンプ・日付表示（SMK-070〜072）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-070 | JST 表示（提出日時） | レポート詳細 | UTC `2026-04-11T18:00:00Z` で submitted されたレポート | Member | レポート詳細 | 1. 提出日時を確認 | 「2026-04-12 03:00」のように JST に変換されて表示される（UTC のままにならない） | 開発者 |
| SMK-071 | 日付フィールドの JST 整合 | 明細追加 | なし | Member | 明細追加モーダル | 1. 日付ピッカーで「今日」を選択 | 日本のローカル日付が初期表示され、保存後の一覧でも同じ日付で表示される（タイムゾーンずれが起きない） | 開発者 |
| SMK-072 | 月の境界 | レポート対象期間 | 開始日 `2026-04-01`、終了日 `2026-04-30` のレポート | Member | レポート詳細 | 1. 対象期間を確認 | 「2026-04-01 〜 2026-04-30」と表示され、JST/UTC の差で月をまたがない | 開発者 |

### 4.8 ページネーション・フィルタ（SMK-080〜084）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-080 | レスポンスタイム軽量スモーク | レポート一覧 | レポート 50 件 | Member | レポート一覧 | 1. 一覧画面を開く 2. 体感の応答速度を確認 | 体感で 1 秒未満に一覧が描画される（NFR-PERF-003 の代替軽量チェック。詳細計測は CRS-083〜088） | 開発者 |
| SMK-081 | ページ送り | レポート一覧 | レポート 30 件（1ページ20件） | Member | レポート一覧 | 1. 「次のページ」を押下 | 21〜30 件目が表示される。URL クエリにページ番号が反映される | 開発者 |
| SMK-082 | ステータスフィルタ後のページリセット | レポート一覧 | レポート 30 件（draft 5件、submitted 25件） | Member | レポート一覧 2 ページ目 | 1. 2ページ目に移動 2. ステータス「draft」でフィルタ | フィルタ後にページが 1 にリセットされ、5 件が表示される | 開発者 |
| SMK-083 | フィルタ条件の URL 反映 | レポート一覧 | レポート 30 件 | Member | レポート一覧 | 1. ステータス「submitted」を選択 2. URL を確認 | URL クエリに `status=submitted` が含まれ、再読込しても条件が維持される | 開発者 |
| SMK-084 | 空状態表示 | レポート一覧 | レポート 0 件のユーザー（`test-member-empty@example.com`） | `test-member-empty@example.com`（テナントA・Member） | レポート一覧 | 1. 一覧を開く | 「経費レポートはまだありません。レポートを作成して経費精算を始めましょう。」のメッセージと EmptyState 内の「レポートを作成」アクション（`/reports/new` への導線）が表示される | 開発者 |

### 4.9 キャッシュ／再読込整合性（SMK-090〜094）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-090 | ヘルスチェック動作確認 | `/health` エンドポイント | なし | （未ログイン） | ブラウザ | 1. `/health` を直接開く | HTTP 200 と JSON ボディ（status: ok）が返却される（NFR-AVAIL-002 の運用前確認） | 開発者 |
| SMK-091 | 承認後の pending 一覧整合 | 承認待ち一覧 | `reportSubmitted` | Approver | 承認待ち一覧 | 1. レポート詳細を開く 2. 承認 3. 一覧に戻る | 承認したレポートが pending 一覧から消えている（キャッシュが古いまま残らない） | 開発者 |
| SMK-092 | 支払後の payable 一覧整合 | 支払待ち一覧 | `reportApproved` | Accounting | 支払待ち一覧 | 1. 詳細を開く 2. 支払完了を記録 3. 一覧に戻る | 支払対象が payable 一覧から消えている | 開発者 |
| SMK-093 | ダッシュボード件数の更新 | ダッシュボード | `reportDraft` | Member | ダッシュボード | 1. 件数を確認 2. レポート作成画面で新規レポートを作成 3. ダッシュボードに戻る | 下書き件数が +1 される（ダッシュボードを再読込しなくても、画面復帰時に最新化される） | 開発者 |
| SMK-094 | ブラウザ再読込（F5）後の状態 | レポート詳細 | `reportSubmitted` | Member | レポート詳細 | 1. F5 で再読込 | 表示中の URL でレポート詳細が再描画される（404 やログイン画面に戻らない） | 開発者 |

### 4.10 確認ダイアログ操作（SMK-095〜098）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-095 | 承認確認ダイアログのコメント入力と承認実行 | レポート詳細（SCR-RPT-004） → 承認 | `reportSubmitted`（他者作成） | Approver | レポート詳細画面 | 1. 「承認する」を押下 2. 確認ダイアログが表示されることを確認 3. コメント欄（任意）に「問題ありません」と入力 4. 「承認する」ボタンを押下 | ダイアログにコメント入力欄（テキストエリア）が表示される。コメント未入力でも「承認する」ボタンは有効。ボタン押下後に disabled + スピナーになり、成功後「レポートを承認しました」トーストが表示される。画面再読み込みでステータスが「承認済み」に変わり、承認コメントが表示される | 開発者 |
| SMK-096 | 却下確認ダイアログの理由必須バリデーションと却下実行 | レポート詳細（SCR-RPT-004） → 却下 | `reportSubmitted`（他者作成） | Approver | レポート詳細画面 | 1. 「却下する」を押下 2. 確認ダイアログが表示されることを確認 3. 却下理由を空のまま「却下する」ボタンを押下 4. 「却下理由を入力してください」のエラーが表示されることを確認 5. 却下理由に「証憑が不足しています」と入力 6. 「却下する」ボタンを押下 | ダイアログに却下理由入力欄（テキストエリア、必須）が表示される。理由未入力時はフィールド下に「却下理由を入力してください」と赤字で表示され、送信されない。理由入力後にボタン押下で disabled + スピナーになり、成功後「レポートを却下しました」トーストが表示される。ステータスが「却下」に変わり、却下理由が表示される | 開発者 |
| SMK-097 | 支払完了確認ダイアログの操作と実行 | レポート詳細（SCR-RPT-004） → 支払完了 | `reportApproved`（他者作成） | Accounting | レポート詳細画面 | 1. 「支払完了にする」を押下 2. 確認ダイアログが表示されることを確認 3. 「支払完了にする」ボタンを押下 | ダイアログに「このレポートの支払完了を記録しますか?」と表示される。ボタン押下後に disabled + スピナーになり、成功後「支払完了を記録しました」トーストが表示される。ステータスが「支払済み」に変わり、支払処理者名と支払完了日が表示される | 開発者 |
| SMK-098 | 削除確認ダイアログの操作とレポート一覧遷移 | レポート詳細（SCR-RPT-004） → 削除 | `reportDraft` | Member | レポート詳細画面 | 1. 「削除」を押下 2. 確認ダイアログに「このレポートを削除しますか? この操作は取り消せません。」と表示されることを確認 3. 「削除する」ボタンを押下 | ボタン押下後に disabled + スピナーになり、成功後「レポートを削除しました」トーストが表示される。レポート一覧画面（SCR-RPT-001）に自動遷移し、削除したレポートが一覧に存在しない | 開発者 |

### 4.11 画面内ナビゲーションリンク（SMK-099〜100）

| チェックID | 確認観点 | 対象画面・対象機能 | 前提データ | 利用アカウント | 開始状態 | 操作手順 | 期待結果 | 確認者 |
|-----------|---------|------------------|----------|--------------|---------|---------|---------|--------|
| SMK-099 | パスワードリセット要求画面の「ログイン画面に戻る」リンク | パスワードリセット要求（SCR-AUTH-003） | なし | （未ログイン） | パスワードリセット要求画面 | 1. 「ログイン画面に戻る」リンクが表示されていることを目視確認 2. リンクを押下 | リンクテキストが画面下部に表示されている。押下するとログイン画面（SCR-AUTH-002）に遷移する | 開発者 |
| SMK-100 | パスワードリセット実行画面の「ログイン画面に戻る」リンク（フォーム状態） | パスワードリセット実行（SCR-AUTH-004） | パスワードリセットメールのリンク（有効なトークン） | （未ログイン） | パスワードリセット実行画面（フォーム表示状態） | 1. フォーム表示状態で「ログイン画面に戻る」リンクが表示されていることを目視確認 2. リンクを押下 | フォーム表示状態の画面下部にリンクテキストが表示されている。押下するとログイン画面（SCR-AUTH-002）に遷移する | 開発者 |

## 5. チェック数サマリ

| カテゴリ | チェック数 |
|---------|----------|
| ロール別導線 | 9 |
| 通信中の挙動 | 5 |
| エラー表示 | 10 |
| 添付ファイルのブラウザ実挙動 | 9 |
| 日本語 UI | 4 |
| レスポンシブ | 6 |
| タイムスタンプ・日付 | 3 |
| ページネーション・フィルタ | 5 |
| キャッシュ／再読込整合 | 5 |
| 確認ダイアログ操作 | 4 |
| 画面内ナビゲーションリンク | 2 |
| **合計** | **62** |
